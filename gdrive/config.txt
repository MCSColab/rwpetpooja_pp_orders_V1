# ============================================
# Google Drive Auto-Uploader Configuration
# ============================================

# Local folder path to monitor for new files
# Use forward slashes (/) or escaped backslashes (\\)
LOCAL_FOLDER_PATH = C:/Work/RippleWalk/UC2/Files/

# Google Drive destination folder ID
# Extract from Google Drive folder URL: https://drive.google.com/drive/folders/<FOLDER_ID>
GDRIVE_FOLDER_ID = 1cyR8NadwZWaTZ8seMao2uCdNfgkawi_W

# Polling interval in seconds (how often to check for new files)
POLL_INTERVAL_SECONDS = 10

# Path to the Google OAuth client secret JSON file
CLIENT_SECRET_FILE = client_secret_649546977597-5vnjtk93r32osbhqshhep1db7rtml45k.apps.googleusercontent.com.json

# Path to store OAuth token (will be created automatically after first auth)
TOKEN_FILE = token.json

# Path to the uploaded files record (JSON file to track processed files)
UPLOADED_RECORD_FILE = uploaded_files.json

# Log file path
LOG_FILE = uploader.log

# ============================================
# Future-Ready Settings
# ============================================

# Enable/Disable file type filtering (true/false)
ENABLE_FILE_FILTER = false

# Allowed file extensions (comma-separated, e.g., .pdf,.docx,.jpg)
# Only used if ENABLE_FILE_FILTER is true
ALLOWED_EXTENSIONS = .pdf,.docx,.xlsx,.jpg,.png,.txt

# Maximum file size in MB (0 = no limit)
MAX_FILE_SIZE_MB = 0

# Enable notifications (future feature)
ENABLE_NOTIFICATIONS = false

# Retry failed uploads (number of attempts)
RETRY_ATTEMPTS = 3

# Delay between retry attempts in seconds
RETRY_DELAY_SECONDS = 5
